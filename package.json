{
  "name": "spotlight-chrome",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/Raj1998/Spotlight-Chrome.git",
  "author": "Raj",
  "license": "MIT",
  "devDependencies": {
    "@types/chrome": "^0.0.132",
    "@types/node": "^14.14.31",
    "typescript": "^4.1.5"
  },
  "scripts": {
    "tsc-watch": "tsc --watch src/*.ts --outDir ./lib",
    "build": "rm -f build.zip && zip -r build.zip ./icons ./lib content.css manifest.json options.html popup.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
